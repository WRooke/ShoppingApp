"""Maintenance scripts (backup/restore). Run as modules from the project root:

    python -m scripts.backup
    python -m scripts.restore [backup_id]

so that ``app`` is importable — see backup.bat / restore.bat.
"""
